<header>
	<div class="logo">
		<a href="{{ path('app_home') }}">
			<img src="{{ asset('img/logo.png') }}" alt="logo Innae">
		</a>
	</div>

	<nav>
		<ul class="listNav">
			<li class="liNav">
				<a href="{{ path('app_order_public') }}">Bêret Personnalisé</a>
			</li>
			<li class="liNav">
				<a href="{{ path('app_training_index') }}">Cours/Ateliers</a>
			</li>
			<li class="liNav">
				<a href="{{ path('app_contact_public') }}">Contact</a>
			</li>

			<li class="liNav login-link">
				{% if app.user and is_granted('ROLE_ADMIN') %}

					<a href="{{ path('backoffice_dashboard') }}">
						<img src="{{ asset('img/IlustrationCedenia.jpg') }}" alt="Admin Avatar" class="avatar">
					</a>
				{% else %}

					<a href="{{ path('app_login') }}">
						<iconify-icon icon="streamline-freehand:login-rectangle" width="32" height="32"></iconify-icon>
					</a>
				{% endif %}
			</li>
		</ul>
	</nav>
</header>
